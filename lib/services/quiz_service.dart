import '../models/quiz.dart';

class QuizService {
  Future<List<Quiz>> fetchQuizzes(String moduleId) async {
    return [];
  }

  Future<void> submitQuiz(String quizId, List<int> userAnswers) async {
    // Envoi des réponses pour évaluation
  }
}
