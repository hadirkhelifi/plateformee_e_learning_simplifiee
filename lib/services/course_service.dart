import 'package:plateforme_e_learning_simplifiee/models/lesson.dart';

import '../models/module.dart';

class CourseService {
  Future<List<Module>> fetchModules() async {
    // Simule une requête API ou une base de données
    return [];
  }

  Future<void> uploadLesson(String moduleId, Lesson lesson) async {
    // Ajouter une leçon à un module
  }
}
