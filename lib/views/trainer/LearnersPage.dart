import 'package:flutter/material.dart';

class LearnersPage extends StatelessWidget {
  const LearnersPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Liste des apprenants et suivi",
        style: TextStyle(fontSize: 18),
      ),
    );
  }
}
